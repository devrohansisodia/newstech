# Installer Package

Owns the dedicated `newstech:install` command, installer-only config, default settings seeding, demo asset publishing, rerunnable demo content installation, and GitHub/demo onboarding support for a fresh NewsTech setup.
