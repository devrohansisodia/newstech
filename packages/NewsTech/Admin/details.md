# Admin Package

Owns the NewsTech admin shell, admin auth flow, shared admin Blade components, admin asset entries, ACL/menu-aware layout wiring, centralized admin-facing module views, and the settings group registry/pages.

The Admin asset bundle now also owns the Tiptap-based rich text editor foundation used by content forms. After frontend dependency changes, rebuild with `npm run build` or `npm run build:admin`.
